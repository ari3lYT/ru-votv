# Polish pass 004: settings and graphics UI

Scope:

* normalized graphics abbreviations: `RHI`, `TAA`, `AA`, `SSR`, `SSGI`, `HDR`, `VSync`, `FOV`;
* fixed bad machine translations around `Screen Space`, `Bloom`, dithering, render distance and upscaling;
* cleaned wording for shadow, rain, volumetric fog, RHI and screen distortion settings;
* extended the glossary with settings/UI terms.

Validation after the pass:

* CSV rows: 19263;
* translated rows: 19263;
* format mismatches for `{...}`, `<...>`, `/image`, `/imagesize`, `/align`: 0;
* bad settings/UI terminology hits in Russian text: 0;
* raw `RMB` / `LMB` / `MMB` in Russian text: 0;
* NBSP / zero-width / BOM in Russian text: 0.
