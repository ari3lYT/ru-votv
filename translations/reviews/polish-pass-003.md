# Polish pass 003: gameplay terminology

Scope:

* unified `drive` as `диск` for signal storage;
* unified `drive box`, `drive rack`, `drive slot`;
* unified `upgrade` as `улучшение`;
* cleaned ATV, server, transformer and data tape wording;
* refreshed several daily-task tutorial strings touched by these terms.

Validation after the pass:

* CSV rows: 19263;
* translated rows: 19263;
* format mismatches for `{...}`, `<...>`, `/image`, `/imagesize`, `/align`: 0;
* bad drive / upgrade terminology hits in Russian text: 0;
* raw `RMB` / `LMB` / `MMB` in Russian text: 0;
* `реквизит` for `prop`: 0;
* NBSP / zero-width / BOM in Russian text: 0.
