# Polish pass 002

Второй ручной проход по обучающим и help-строкам.

Что исправлено:

* Переписаны подсказки про `GRAB` / `HOLD`: захват и удержание теперь объясняются по-русски.
* Исправлены базовые controls, подсказки про рамку взаимодействия и строки с `USE`.
* Переписаны описания видеокамер и предупреждение про WIP/низкий FPS.
* Исправлены короткие кривые строки вроде `HOLD lets...`, `Release E instead of LMB...`,
  `Blend between full TAA...`, ошибки сохранения слота.
* `(WIP)` и `[WIP]` в русских строках заменены на `(в разработке)` / `[в разработке]`.

Проверки после прохода:

* Несовпадения `{...}` и markup-токенов: `0`.
* `RMB/LMB/MMB` в русских строках: `0`.
* Сломанные `/imagesize` / `/align`: `0`.
