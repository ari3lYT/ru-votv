# 🇷🇺 Перевод *Voices of the Void* (100%)

> **Версия a09b_0004 не поддерживается.
> Технически включить перевод в неё невозможно.**

---

## 📦 Файлы из архива

* **`ZZ_GameRuPatch_P.pak`** — перевод игрового контента (*Game.locres*).
* **`ZZ_EngineRuPatch_P.pak`** — перевод интерфейса движка (*Engine.locres*).
  Если перевод меню экрана/движка не нужен, этот файл можно не класть.

---

## 🔧 Установка

1. Открой папку с установленной игрой.
2. Перейди в:

   ```
   WindowsNoEditor/VotV/Content/Paks/
   ```

   Пример пути:
   `C:/Voices Of The Void/a09_0015/WindowsNoEditor/VotV/Content/Paks`
3. Скопируй в эту папку нужные `.pak` файлы **без переименования** — порядок загрузки зависит от имени.
4. Запускай игру — перевод подключится сам.
5. Чтобы убрать перевод, просто удали эти `.pak`.

---

## 🛠 Как делался перевод

* Исходные тексты извлечены из оригинальных UE4-локализаций (`.locres`).
* Перевод генерировался автоматически (ChatGPT-5.1), без ручной корректуры — возможны неточности.
* Всё собрано обратно в `.locres` и упаковано в `.pak` с корректным mount-point, чтобы перекрывать оригинальные файлы при запуске.

---

## 🧪 Поддержка и тесты

### Проверено на системах

* Windows 11 Pro 24H2 (OS Build 26100)
* Proton 9.0-4

### Рабочие версии игры

* ✔️ **a09_0015** — полностью поддерживается
* ❌ **a09b_0004** — **перевод не работает**

#### Почему a09b_0004 недоступна

Разработчик поменял формат паков:
`VotV-WindowsNoEditor.pak` больше не обычный UE4-pak.
Теперь в `VotV/index` хранится whitelist с **двумя подписанными пакетами**, и движок монтирует **только их**.

Наши файлы (`ZZ_GameRuPatch_P.pak`, `ZZ_EngineRuPatch_P.pak`) — обычные UE4-пакеты **без цифровой подписи**, поэтому движок их игнорирует.
Добавить наши записи в whitelist **невозможно без приватного ключа** или хаков exe.

Итог: перевод работает на **a09_0015 и старее**, но начиная с **a09b_0004** его подключить нельзя, пока разработчик не включит поддержку модов или не подпишет сторонние пакеты.

---

## 🤝 Вклад

Хочешь помочь? Делай правки, улучшай текст, открывай форк — вклад приветствуется.
